{
  "name": "property-video-service",
  "version": "1.0.0",
  "description": "Receives project data from Lovable, generates AI video clips, stitches them with FFmpeg, overlays the template, mixes audio, and returns the finished video.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "express": "^4.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
